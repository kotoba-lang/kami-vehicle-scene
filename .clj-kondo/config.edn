;; vehicle-scene.garage/if-ok binds its [sym expr sym expr ...] pairs exactly
;; like `let`, then a body -- lint-as let so clj-kondo sees the bound symbols.
{:lint-as {vehicle-scene.garage/if-ok clojure.core/let}}
